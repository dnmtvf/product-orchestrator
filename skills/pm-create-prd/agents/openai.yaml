interface:
  display_name: "PM Create PRD"
  short_description: "Create PRD from discovery and run PRD approval gate"
  default_prompt: "Use $pm-create-prd to build a PRD from the discovery summary and gate it on explicit approval."
